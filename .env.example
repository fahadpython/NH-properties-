# Google Sheets Setup
# Create a Google Sheet and copy its ID from the URL
GOOGLE_SHEET_ID=

# Create a Service Account in Google Cloud Console
# Download the JSON key file and paste its content here (or as a string)
GOOGLE_SERVICE_ACCOUNT_JSON=
